/**
 * Authentication router boundary.
 *
 * Procedures are intentionally added in the auth-router implementation batch;
 * this module gives the package a stable route-owned composition point.
 */
export const authRouter = undefined;
