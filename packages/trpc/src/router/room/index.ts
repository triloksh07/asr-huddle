/**
 * Room router boundary.
 *
 * Room procedures are intentionally added in the room-router implementation
 * batch; this module gives the package a stable route-owned composition point.
 */
export const roomRouter = undefined;
